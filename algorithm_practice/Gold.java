import java.util.*;

public class Gold {

	public static void main(String[] args) {
		Scanner scanner = new Scanner(System.in);
		int numCase = scanner.nextInt();
		
		while(numCase-- > 0) {
			int numBag = scanner.nextInt();
			int[] golds = new int[numBag];
			
			for (int i = 0; i < numBag; i++) {
				golds[i] = scanner.nextInt();
			}
			
			
		}
	}

}
